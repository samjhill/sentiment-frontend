# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:11.3

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/mongo:3.4.4

    working_directory: ~/repo

    steps:
      - checkout

      - run:
         name: Install Dependencies
         command: yarn install

      - run:
           name: Build Static Site
           command: CI=false yarn run build

      - run:
          name: SCP build files to Droplet
          command: scp -r -o StrictHostKeyChecking=no ~/repo/build/. $DROPLET_USER@$DROPLET_IP:~/sentiment/frontend/build/
      #
      # - run:
      #     name: SSH into Droplet, restart server
      #     command: ssh -v $DROPLET_USER@$DROPLET_IP "cd ~/sentiment/frontend; forever restartall"
